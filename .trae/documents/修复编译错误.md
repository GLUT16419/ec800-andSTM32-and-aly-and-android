我需要修复两个编译错误：

1. **Suspension functions can only be called within coroutine body**

   * 问题：在第182行，`withContext(Dispatchers.Main)`被调用在回调函数中，而回调函数不是协程体

   * 解决方案：修改`loadAndDisplayData()`方法，使用协程的方式处理异步操作，而不是回调函数

2. **Argument type mismatch: actual type is 'com.example.dgb.data.StatisticsAnalyzer.TimeRange', but 'kotlin.Pair\<kotlin.Long, kotlin.Long>' was expected**

   * 问题：`StatisticsData`类的`timeRange`参数期望是`Pair<Long, Long>`类型，但实际传入的是`StatisticsAnalyzer.TimeRange`类型

   * 解决方案：将`StatisticsData`类的`timeRange`参数类型改为`StatisticsAnalyzer.TimeRange`，或者将`timeRange`对象转换为`Pair`类型

修复步骤：

1. 修改`loadAndDisplayData()`方法，使用协程处理数据加载
2. 修改`StatisticsData`类的`timeRange`参数类型，或修改调用处的类型转换
3. 确保所有相关代码都能正确编译

